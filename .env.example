# Template of every variable the app reads. Copy to .env and adjust.
# Keep in sync with src/vite-env.d.ts.

# 'true' enables the AzerothJS devtools panel in dev; anything else disables it.
VITE_DEVTOOLS=true
